# 3D 파일 문서 (Box3.jsx / D3test.tsx)

> 3D 박스 모델을 화면에 띄우고, 애니메이션을 재생하고, 회전시키는 코드의 구조와 의미를 정리한 문서.
> 나중에 다시 봐도 "이 줄이 왜 있는지" 알 수 있도록 각 요소를 역할 중심으로 설명한다.

---

## 1. 전체 구조 한눈에 보기

```
src/
├─ components/
│  ├─ D3test.tsx      ← 화면(Canvas), 조명, 버튼, 카메라 조작을 담당하는 "무대"
│  └─ Box3.jsx        ← 3D 모델(.glb)을 불러와 애니메이션을 붙이는 "배우"
└─ public/models/
   ├─ hiandlowTest333.glb   ← Blender에서 만든 3D 모델 + 애니메이션 파일
   └─ Wood-6986.jpg         ← 나무 텍스처 (현재는 사용 안 함)
```

| 파일 | 한 줄 요약 |
|---|---|
| `D3test.tsx` | 3D 장면을 만든다. 조명, 카메라, 바닥, 버튼, 회전 토글이 여기 있다. |
| `Box3.jsx` | `.glb` 모델을 불러와서 메시를 그리고, 받은 애니메이션 이름대로 재생한다. |
| `hiandlowTest333.glb` | 모델(메시 이름 `low`)과 애니메이션(`lowAction`)이 들어 있는 파일. |

> 참고: `Box3 copy.jsx`, `Box3 copy 2.jsx`, `Box3 copy 3.jsx`는 `Box3.jsx`의 이전 버전(백업)으로 보이며, 현재 화면에는 `Box3.jsx`만 쓰인다.

### 데이터가 흐르는 방향

```
[버튼 클릭]
   │  setAnimationName / setLoop / setRotating   (D3test의 state 변경)
   ▼
[D3test.tsx]  ── props(currentAction, isLooping) ──▶  [Box3.jsx]
   │                                                     │
   │ enabled={rotating}                                  │ useEffect: 애니메이션 시작/설정
   ▼                                                     │ useFrame:  1회 재생 끝 감시
[Rotator] ── 자식으로 Box3를 감싸서 Y축 회전 ────────────┘
```

---

## 2. 사용된 라이브러리

| 라이브러리 | 역할 |
|---|---|
| `three` | 3D 엔진 본체. 애니메이션 상수(`THREE.LoopRepeat` 등)를 쓰기 위해 import. |
| `@react-three/fiber` | Three.js를 React 컴포넌트처럼 쓰게 해 준다. `Canvas`, `useFrame`이 여기 있다. |
| `@react-three/drei` | 자주 쓰는 도구 모음. `useGLTF`, `useAnimations`, `useTexture`, `OrbitControls`. |
| `gltfjsx` | `.glb` 파일을 React 컴포넌트 코드로 자동 변환해 주는 도구. `Box3.jsx`의 뼈대가 이걸로 생성됨. |

---

## 3. Box3.jsx 상세 설명

### 3-1. 파일 머리말 (1~4줄)

```js
/* Auto-generated by: https://github.com/pmndrs/gltfjsx
   Command: npx gltfjsx@6.5.3 hiandlowTest333.glb */
```

- `gltfjsx`가 자동 생성한 파일이라는 표시.
- 다시 변환 명령을 실행하면 **직접 수정한 내용(애니메이션 제어 코드 등)이 덮어써질 수 있으니** 주의.

### 3-2. 컴포넌트 선언과 props

```js
export function Box3({ currentAction, isLooping = true, ...props })
```

| props | 타입 | 의미 |
|---|---|---|
| `currentAction` | 문자열 또는 `null` | 재생할 애니메이션 이름 (예: `"lowAction"`). `null`이면 아무것도 재생 안 함. |
| `isLooping` | boolean (기본 `true`) | `true`면 반복 재생, `false`면 1회 재생. |
| `...props` | 나머지 | `position`, `rotation` 등 다른 값은 그대로 바깥 `<group>`에 전달된다. |

### 3-3. 리소스 불러오기 (12~18줄)

```js
const group = useRef();
const { nodes, materials, animations } = useGLTF("/models/hiandlowTest333.glb");
const { actions } = useAnimations(animations, group);
const newTexture = useTexture("/models/Wood-6986.jpg");
```

| 코드 | 의미 |
|---|---|
| `useRef()` (`group`) | 아래 렌더링의 `<group>`을 가리키는 손잡이. 애니메이션이 "어떤 오브젝트를 움직일지" 알려주는 기준이 된다. |
| `useGLTF(경로)` | `.glb`를 불러온다. 결과 3가지: |
| ↳ `nodes` | 모델 안의 메시들 (이름으로 접근, 예: `nodes.low`). |
| ↳ `materials` | 모델 안의 재질들 (예: `materials["Material.001"]`). |
| ↳ `animations` | 모델에 들어 있는 애니메이션 클립 목록. |
| `useAnimations(animations, group)` | 클립들을 재생할 수 있는 `actions` 객체로 만들어 준다. `actions["lowAction"]`처럼 이름으로 꺼낸다. |
| `useTexture(경로)` | 이미지 파일을 텍스처로 불러온다. **현재는 `map={newTexture}`가 주석 처리되어 있어 화면에 적용되지 않는다.** (파일은 로드만 됨) |

### 3-4. useEffect: 애니메이션 시작/설정 (20~42줄)

**언제 실행되나?** `currentAction`, `isLooping`, `actions` 중 하나가 바뀔 때마다.

```js
const action = actions[currentAction];
if (action) {
  action.reset().stop();                       // ① 완전 초기화, 0프레임에서 정지
  if (isLooping) {
    action.setLoop(THREE.LoopRepeat, Infinity); // ② 무한 반복
    action.clampWhenFinished = false;
  } else {
    action.setLoop(THREE.LoopOnce, 1);          // ② 1회만 재생
    action.clampWhenFinished = true;            //    끝나면 마지막 프레임에서 멈춤
  }
  action.fadeIn(0.2).play();                    // ③ 0.2초에 걸쳐 부드럽게 시작
}
return () => { if (action) action.fadeOut(0.2); }; // ④ 정리(cleanup)
```

| 단계 | 의미 |
|---|---|
| ① `reset().stop()` | 이전 재생 흔적을 지우고 처음 상태로 돌린다. 재시작할 때 이어서 재생되는 것을 방지. |
| ② 루프 설정 | 반복(`LoopRepeat`)인지 1회(`LoopOnce`)인지 지정. |
| `clampWhenFinished` | `true`면 1회 재생이 끝난 뒤 마지막 자세를 유지. (단, 아래 useFrame이 다시 처음으로 돌려 버린다.) |
| ③ `fadeIn(0.2).play()` | 갑자기 튀지 않게 0.2초 페이드인 후 재생. |
| ④ cleanup | 값이 바뀌거나 컴포넌트가 사라질 때, 0.2초 페이드아웃하며 이전 애니메이션을 정리. |

### 3-5. useFrame: 1회 재생 종료 감시 (44~57줄)

**언제 실행되나?** 화면이 그려질 때마다 (보통 초당 60번).

```js
useFrame(() => {
  const action = actions[currentAction];
  if (!action || isLooping) return;            // 반복 모드거나 액션이 없으면 아무것도 안 함

  const duration = action.getClip().duration;  // 애니메이션 전체 길이(초)
  if (action.time >= duration - 0.01) {        // 거의 끝까지 재생됐으면
    action.stop();                             // 정지 + 0프레임(처음 자세)으로 되돌림
  }
});
```

- **목적**: "1회 재생 후 마지막 자세에 멈추는" 기본 동작 대신 **재생이 끝나면 처음 자세로 되돌리기** 위해 직접 감시한다.
- `duration - 0.01`: 부동소수점 오차 때문에 정확히 끝 시간에 도달하지 못할 수 있어 0.01초 여유를 둔 것.
- `action.stop()`은 내부적으로 `reset()`을 호출하므로 시간이 0으로 돌아간다. 그래서 이 조건이 무한히 반복 실행되지는 않는다.

### 3-6. 렌더링 구조 (59~70줄)

```jsx
<group ref={group} {...props} dispose={null}>
  <group name="Scene">
    <mesh name="low" geometry={nodes.low.geometry}>
      <meshStandardMaterial {...materials["Material.001"]} />
    </mesh>
  </group>
</group>
```

| 요소 | 의미 |
|---|---|
| 바깥 `<group ref={group}>` | 이 모델 전체를 묶는 상자. 애니메이션의 기준 객체이고, 바깥에서 준 `position` 등 props를 받는다. |
| `dispose={null}` | 컴포넌트가 사라져도 GLTF 리소스를 자동 삭제하지 않는다. (캐시된 모델을 다시 쓸 수 있게) |
| `<group name="Scene">` | Blender 씬 루트를 그대로 옮긴 이름. |
| `<mesh name="low">` | 실제로 보이는 박스. **`name="low"`는 애니메이션이 이 이름으로 대상을 찾기 때문에 바꾸면 안 된다.** |
| `geometry={nodes.low.geometry}` | 박스의 모양(꼭짓점 데이터)을 GLB에서 가져온다. |
| `meshStandardMaterial` | 빛(조명)의 영향을 받는 표준 재질. 밝기가 조명 방향에 따라 달라지는 이유가 이것. |
| `{...materials["Material.001"]}` | GLB에 들어 있던 재질의 속성값(색, 거칠기 등)을 복사해서 적용. |

### 3-7. 마지막 줄

```js
useGLTF.preload("/models/hiandlowTest333.glb");
```

- 컴포넌트가 화면에 나오기 **전에 미리** 모델을 다운로드해 둔다. 처음 표시될 때 로딩 지연을 줄인다.

---

## 4. D3test.tsx 상세 설명

### 4-1. state (상태값) 정리

| state | 초기값 | 의미 | 바꾸는 곳 |
|---|---|---|---|
| `animationName` | `null` | 지금 재생할 애니메이션 이름. `Box3`의 `currentAction`으로 전달됨. | 재생 / 정지 버튼 |
| `loop` | `true` | 반복 재생 여부. `Box3`의 `isLooping`으로 전달됨. | 루프 상태 버튼 |
| `rotating` | `true` | 모델이 Y축으로 계속 도는지 여부. `Rotator`의 `enabled`로 전달됨. | 모델 회전 버튼 |

### 4-2. 버튼

| 버튼 | 동작 |
|---|---|
| **lowAction 재생하기** | `handlePlay("lowAction")` 실행 |
| **정지** | `animationName`을 `null`로 → 애니메이션 정지 |
| **루프 상태** | `loop` 토글. 초록(반복 재생) / 회색(한번만 재생) |
| **모델 회전** | `rotating` 토글. 초록(ON) / 회색(OFF) |

### 4-3. handlePlay 의 `setTimeout(…, 0)` 트릭

```js
const handlePlay = (actionName) => {
  setAnimationName(null);                          // ① 먼저 null로 (이전 재생 종료)
  setTimeout(() => setAnimationName(actionName), 0); // ② 다음 틱에 다시 이름 지정
};
```

- 같은 이름(`"lowAction"`)을 다시 눌러도 state 값이 **변하지 않으면** `Box3`의 `useEffect`가 실행되지 않아 재시작이 안 된다.
- 그래서 `null` → 이름 순서로 강제로 값을 바꿔서 **재생을 처음부터 다시 시작**하게 만든다.

### 4-4. Rotator 컴포넌트 (모델 회전용)

```jsx
const Rotator = ({ children, speed = 0.5, enabled = true }) => {
  const ref = useRef();
  useFrame((_, delta) => {
    if (!enabled) return;                       // OFF면 회전값을 올리지 않음 (각도는 유지)
    ref.current.rotation.y += delta * speed;    // Y축 회전
  });
  return <group ref={ref}>{children}</group>;
};
```

| 항목 | 의미 |
|---|---|
| `children` | 감싸서 돌릴 대상 (여기서는 `Box3`). |
| `speed` | 초당 회전량(라디안). 기본 0.5 → 한 바퀴에 약 12.6초. |
| `enabled` | `false`면 멈춤. 멈춘 각도에서 다시 켜면 이어서 회전. |
| `delta` | 이전 프레임과의 시간 차이(초). 곱해 주면 **컴퓨터 성능/주사율과 상관없이 같은 속도**로 돈다. |
| **왜 바깥 group인가** | `Box3` 내부의 애니메이션 group과 겹치지 않고 독립적으로 회전시키기 위해. |

> `useFrame`은 `<Canvas>` **안쪽** 컴포넌트에서만 쓸 수 있어서 `Rotator`를 별도 컴포넌트로 분리했다.

### 4-5. Canvas 설정

```jsx
<Canvas shadows camera={{ position: [3, 3, 10], fov: 45 }} style={{ background: '#1a1a1a' }}>
```

| 설정 | 의미 |
|---|---|
| `shadows` | 그림자 기능 켜기. |
| `camera.position [3, 3, 10]` | 카메라 시작 위치 (x, y, z). |
| `fov: 45` | 화각. 작을수록 망원(좁게), 클수록 광각(넓게). |
| `background` | 캔버스 배경색 (어두운 회색). |

### 4-6. 조명 (3종 세트)

| 조명 | 위치 | 세기 | 역할 |
|---|---|---|---|
| `ambientLight` | 없음 | 0.3 | 모든 면에 깔리는 **최소 밝기**. 없으면 그림자 면이 완전히 까맣게 된다. |
| `directionalLight` | `[-5, 7, 5]` | 3 | **메인 조명**. 햇빛처럼 한 방향으로 오는 평행광. 그림자를 만든다 (`castShadow`). |
| `pointLight` | `[-5, -2, -5]` | 0.5 | **보조 조명**. 반대편(아래·뒤)을 약하게 밝혀 입체감 보완. |

- `shadow-mapSize={[2048, 2048]}`: 그림자 해상도. 클수록 선명하지만 무겁다.
- `shadow-bias={-0.0001}`: 그림자 표면에 생기는 줄무늬(shadow acne) 방지용 보정값.

### 4-7. 바닥 (그림자 받는 판)

```jsx
<mesh rotation={[-Math.PI / 2, 0, 0]} position={[0, -1, 0]} receiveShadow>
  <planeGeometry args={[100, 100]} />
  <shadowMaterial opacity={0.4} />
</mesh>
```

- `planeGeometry`는 원래 세로로 서 있어서 `-π/2` 회전으로 **눕힌다**.
- `shadowMaterial`: 평소엔 투명하고 **그림자가 닿는 곳만** 반투명 검정으로 보이는 재질.
- `receiveShadow`: 이 메시가 그림자를 받겠다는 표시.

### 4-8. OrbitControls (카메라 조작)

| 옵션 | 의미 |
|---|---|
| `enableDamping`, `dampingFactor={0.05}` | 마우스를 놓아도 살짝 관성이 남아 부드럽게 멈춤. |
| `minDistance={2}`, `maxDistance={10}` | 확대/축소 가능한 거리 범위. |

- **주의**: OrbitControls는 **카메라만** 움직인다. 조명과 모델은 그대로다.

---

## 5. 밝기가 "고정"처럼 보이는 이유

- 조명은 **월드 좌표에 고정**되어 있고, 밝기는 **매 프레임 실시간 계산**된다. (한 번 굽는 방식이 아님)
- 면의 밝기 = 그 면이 향한 방향 ↔ 조명 방향의 각도로 결정된다.
- 마우스로 카메라를 돌려도 조명과 모델의 상대 위치는 그대로라서 밝은 면/어두운 면이 안 바뀐다.
- **밝기가 바뀌는 경우**: 모델이 회전할 때 (Rotator, 애니메이션), 조명 위치/세기를 바꿀 때.
- 초기 카메라(`[3,3,10]`)와 메인 조명(`[-5,7,5]`)이 둘 다 앞·위쪽에 있어서 처음 보이는 3면이 우연히 밝은 면과 겹친다.

---

## 6. 자주 하는 수정 방법 (치트시트)

| 하고 싶은 것 | 어디를 고치나 |
|---|---|
| 회전 속도 변경 | `D3test.tsx`의 `<Rotator speed={1.5}>` |
| 회전 기본값을 OFF로 | `useState(true)` → `useState(false)` (`rotating`) |
| 전체를 더 밝게 | `ambientLight`의 `intensity` 올리기 |
| 뒷면도 밝게 | 반대편에 `directionalLight` 추가 또는 `<Environment preset="city" />` |
| 나무 텍스처 입히기 | `Box3.jsx`의 `// map={newTexture}` 주석 해제 |
| 다른 애니메이션 재생 | 버튼에 `handlePlay("다른이름")` 추가 (Blender에서 정한 Action 이름) |
| 카메라 시작 위치 | `Canvas`의 `camera.position` |
| 확대/축소 범위 | `OrbitControls`의 `minDistance` / `maxDistance` |

---

## 7. 알려진 문제 / 개선할 점 (TODO)

1. **박스 그림자가 안 생김**
   `Box3.jsx`의 `<mesh name="low">`에 `castShadow`가 없다. 추가해야 바닥에 그림자가 맺힌다.
   ```jsx
   <mesh name="low" geometry={nodes.low.geometry} castShadow>
   ```
2. **타입 오류 (D3test.tsx)**
   `useState(null)`에 문자열을 넣어서 TS 오류(TS2345)가 난다. 아래처럼 고치면 해결.
   ```ts
   const [animationName, setAnimationName] = useState<string | null>(null);
   ```
3. **`Box3.jsx`의 타입 선언 없음 (TS7016)**
   `.jsx` 파일이라 TS가 타입을 모른다는 경고. `.tsx`로 바꾸거나 선언 파일을 만들면 사라진다.
4. **`useTexture`가 불필요하게 로드됨**
   `newTexture`는 지금 쓰이지 않는데 파일을 계속 불러온다. 안 쓸 거면 삭제, 쓸 거면 `map` 주석 해제.
5. **`materials[...]` 스프레드 방식**
   `{...materials["Material.001"]}`는 속성을 복사하는 방식이다. gltfjsx의 기본 방식인 `material={materials["Material.001"]}`(재질 객체 그대로 사용)가 더 안전하다. 재질이 이상하게 보이면 이 부분을 의심.
6. **애니메이션과 회전이 겹칠 수 있음**
   `lowAction`이 박스를 직접 회전시키는 애니메이션이라면 `Rotator`의 회전과 합쳐져서 움직임이 복합적으로 보인다.
7. **`gltfjsx` 재실행 주의**
   `Box3.jsx`를 다시 자동 생성하면 직접 넣은 애니메이션 제어 코드가 사라진다. 재생성 전에 반드시 백업.

---

## 8. 용어 사전

| 용어 | 뜻 |
|---|---|
| **GLB / GLTF** | 3D 모델 파일 형식. 모양, 재질, 애니메이션을 한 파일에 담는다. (`.glb`는 바이너리 한 덩어리) |
| **Mesh (메시)** | 화면에 보이는 3D 물체. geometry(모양) + material(재질)로 구성. |
| **Geometry** | 물체의 모양 (꼭짓점, 면 데이터). |
| **Material** | 물체의 표면 재질 (색, 거칠기, 금속성 등). |
| **Group** | 여러 오브젝트를 묶는 빈 상자. 묶어서 함께 이동/회전시킬 수 있다. |
| **Action (애니메이션 액션)** | 애니메이션 클립을 재생·정지·반복 제어하는 손잡이. |
| **Clip** | 애니메이션 데이터 자체 (시간에 따른 움직임 기록). |
| **useFrame** | 매 프레임마다 실행되는 훅. 움직임·감시 로직에 쓴다. |
| **delta** | 이전 프레임 이후 흐른 시간(초). 속도를 일정하게 만드는 데 쓴다. |
| **라디안 (rad)** | 각도 단위. π(≈3.14) = 180°, 2π = 360°. |
| **법선 (normal)** | 면이 바라보는 방향. 조명 밝기 계산의 기준이 된다. |
| **Ambient / Directional / Point Light** | 전체광 / 평행광(햇빛) / 점광(전구). |
| **OrbitControls** | 마우스로 카메라를 회전·확대·이동하는 도구. |
| **dispose** | 3D 리소스(메모리)를 해제하는 것. `dispose={null}`은 자동 해제를 막는다. |
